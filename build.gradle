

allprojects {
	println "master build applied to: ${project.name}"

	group = 'com.bic64'
	version = '0.0.1-SNAPSHOT'

	apply plugin: 'java'
	apply plugin: 'idea'

	ext {
		springBootVersion = '2.0.4.RELEASE'
	}

	repositories {
		mavenLocal()
		mavenCentral()
	}

}

